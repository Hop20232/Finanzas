{
  "name": "finanzas-familiares",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test tests/**/*.test.cjs"
  }
}
